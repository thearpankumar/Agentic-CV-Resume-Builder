# Streamlit Cloud Secrets Configuration
# Copy this to secrets.toml and update with your cloud database URL

[database]
DATABASE_URL = "postgresql://username:password@host:port/database"

# Optional: Default Groq API key (users can still input their own)
[api]
GROQ_API_KEY = ""

# Application settings
[app]
DEBUG = false
